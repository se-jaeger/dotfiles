LICENSE
README.md

{{ if eq .chezmoi.os "linux" -}}
    Library
    .cspell
    .config/wtf
{{ end -}}

{{ if or (eq .chezmoi.hostname "mail") (eq .chezmoi.os "darwin") -}}
    .config/motd-update
    .config/pyznap
    .config/containerd
{{ end -}}
